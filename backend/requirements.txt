# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
geoalchemy2==0.16.0
psycopg[binary]==3.2.3

# Spatial / analytics
h3==4.2.1

# Crypto (device signature verification, and Google sign-in)
cryptography==44.0.0
# Verifies Google's RS256 id tokens against their published keys, and signs
# this platform's own session cookies. PyJWKClient fetches and caches the key
# set, so key rotation at Google needs no action here.
pyjwt[crypto]==2.10.1

# Background jobs
celery==5.4.0
redis==5.2.1

# Fetches a credited user's profile photo once, so the public sign-in page
# serves it from this platform instead of asking every visitor's browser to
# request it from Google. It is already present transitively; pinned here
# because app code imports it directly and a transitive dependency is one
# upgrade away from vanishing.
httpx==0.28.1

# Utilities
python-multipart==0.0.20
orjson==3.10.13
structlog==24.4.0
